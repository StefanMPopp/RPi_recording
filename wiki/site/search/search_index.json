{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"RPi Recording \u2014 Field Guide","text":"<p>This guide covers everything needed to set up, maintain, and use the insect video recording rigs.</p>"},{"location":"#how-this-guide-is-organised","title":"How this guide is organised","text":"<p>The rigs are managed through two separate apps, each with a different audience and purpose.</p> Manager appRecorder app <p>Who uses it: the person responsible for the rigs (you).</p> <p>What it does: installs software on new Pis, updates all Pis when the code changes, and handles all maintenance tasks.</p> <p>Where it runs: on the dev Pi only. Day-to-day users never need to open it.</p> <p>\u2192 See Setup &amp; maintenance</p> <p>Who uses it: anyone running an experiment.</p> <p>What it does: configures recording parameters, starts and stops recordings, and transfers data to the analysis machine.</p> <p>Where it runs: on each deployed Pi, accessible from any browser on the same network.</p> <p>\u2192 See Recording</p>"},{"location":"#hardware-overview","title":"Hardware overview","text":"<p>Each rig consists of:</p> <ul> <li>Raspberry Pi 4</li> <li>HQ camera module with 16 mm lens</li> <li>LED panels on an aluminium extrusion frame</li> <li>~30 \u00d7 20 cm recording arena</li> </ul> <p>Eight identical rigs are deployed. One additional Pi serves as the dev Pi \u2014 it runs the Manager app and is the only machine used for software development and maintenance.</p>"},{"location":"recording/overview/","title":"Recording overview","text":"<p>This section covers everything needed to run a recording session using the Recorder app \u2014 a lightweight interface accessible from any browser on the same network as the recording Pis.</p>"},{"location":"recording/overview/#the-recorder-app","title":"The Recorder app","text":"<p>Each deployed Pi runs the Recorder app automatically on boot. You do not need to install, update, or configure it \u2014 that is handled by the Manager app.</p> <p>To access a rig's Recorder app, open a browser on your analysis machine and go to:</p> <pre><code>http://pi1.local:8080   # replace pi1 with the hostname of the rig you want\n</code></pre> <p>Note</p> <p>Your analysis machine must be on the same Wi-Fi or ethernet network as the Pis.</p>"},{"location":"recording/overview/#what-the-recorder-app-does","title":"What the Recorder app does","text":"<ol> <li>Accepts recording parameters (arena dimensions, species, experimenter, etc.)</li> <li>Starts and stops the camera recording</li> <li>Saves video files to a timestamped folder on the Pi</li> <li>Transfers completed recordings to your analysis machine</li> </ol>"},{"location":"recording/overview/#before-your-first-session","title":"Before your first session","text":"<ul> <li>Confirm the Pi is powered on and the LED panels are running</li> <li>Check the camera view and focus in the Recorder app's live preview</li> <li>Fill in the experiment config for this session</li> </ul> <p>\u2192 See Run a recording session for the full walkthrough.</p>"},{"location":"recording/record/","title":"Run a recording session","text":"<p>Placeholder \u2014 in progress</p> <p>This page will document the Recorder app once it has been designed and built. The sections below outline what will be covered.</p>"},{"location":"recording/record/#opening-the-recorder-app","title":"Opening the Recorder app","text":"<p>How to access the app from a browser on the analysis machine. What the interface looks like.</p>"},{"location":"recording/record/#setting-up-an-experiment","title":"Setting up an experiment","text":"<p>The parameters you fill in before each session:</p> <ul> <li>Arena identifier</li> <li>Species</li> <li>Experimenter name</li> <li>Any additional experiment-specific settings</li> </ul>"},{"location":"recording/record/#checking-the-camera","title":"Checking the camera","text":"<p>How to use the live preview to verify focus, framing, and lighting before recording.</p>"},{"location":"recording/record/#starting-and-stopping-a-recording","title":"Starting and stopping a recording","text":"<p>Button walkthrough. What happens on the Pi while recording is running. How files are named and where they are saved.</p>"},{"location":"recording/record/#transferring-data","title":"Transferring data","text":"<p>How to initiate a transfer from the Pi to the analysis machine. Expected transfer times. Where files land on the analysis machine.</p>"},{"location":"recording/record/#troubleshooting","title":"Troubleshooting","text":"<p>Common issues during recording and how to resolve them.</p>"},{"location":"setup/add_pi/","title":"Step 2: Add a new Pi","text":"<p>This page covers everything needed to bring a new recording Pi into the fleet \u2014 from a blank SD card to a fully configured rig ready to record.</p> <p>The process has two parts:</p> <ol> <li>Manual preparation \u2014 flash the Pi, assign it a static IP, and copy the SSH key.    This requires physical access to the Pi and your router.</li> <li>Bootstrapping via the Manager app \u2014 installs all software on the Pi automatically.</li> </ol>"},{"location":"setup/add_pi/#part-1-manual-preparation","title":"Part 1 \u2014 Manual preparation","text":""},{"location":"setup/add_pi/#11-flash-the-pi","title":"1.1 Flash the Pi","text":"<ol> <li>Open Raspberry Pi Imager on any computer.</li> <li>Select Raspberry Pi OS Lite (64-bit) \u2014 the Lite version has no desktop, which saves    resources on a Pi that only records video.</li> <li>Select your SD card.</li> <li>Click the gear icon and set:<ul> <li>Hostname: <code>pi1</code> (increment for each new Pi: <code>pi2</code>, <code>pi3</code> \u2026)</li> <li>Enable SSH: checked</li> <li>Username: <code>pi</code></li> <li>Same password as the dev Pi</li> <li>Configure Wi-Fi if not using ethernet</li> </ul> </li> <li>Flash and insert the card.</li> </ol>"},{"location":"setup/add_pi/#12-assign-a-static-ip-address","title":"1.2 Assign a static IP address","text":"<p>A static IP means the Pi always has the same network address. Ansible needs this to find each Pi reliably.</p> <ol> <li>Power on the Pi and let it connect to the network (give it ~60 seconds).</li> <li>Log into your router's admin panel (usually at <code>192.168.1.1</code> or <code>192.168.0.1</code> in a browser).</li> <li>Find the Pi in the connected devices list \u2014 it will appear as <code>pi1</code> (or the hostname you set).</li> <li> <p>Assign it a reserved/static IP. Suggested scheme:</p> Pi Hostname Static IP Dev Pi <code>devpi</code> <code>192.168.1.100</code> Pi 1 <code>pi1</code> <code>192.168.1.101</code> Pi 2 <code>pi2</code> <code>192.168.1.102</code> \u2026 \u2026 \u2026 </li> <li> <p>Save and restart the router if prompted.</p> </li> </ol>"},{"location":"setup/add_pi/#13-copy-the-ssh-key-to-the-new-pi","title":"1.3 Copy the SSH key to the new Pi","text":"<p>Run this from the dev Pi terminal, replacing the IP with the one you just assigned:</p> <pre><code>ssh-copy-id -i ~/.ssh/insect_tracker.pub pi@192.168.1.101\n</code></pre> <p>You will be prompted for the Pi's password once. After that, the dev Pi can connect without a password \u2014 which is what Ansible needs.</p> <p>Verify it worked:</p> <pre><code>ssh -i ~/.ssh/insect_tracker pi@192.168.1.101\n</code></pre> <p>You should land directly in a terminal on the new Pi. Type <code>exit</code> to return.</p>"},{"location":"setup/add_pi/#14-add-the-pi-to-the-inventory-file","title":"1.4 Add the Pi to the inventory file","text":"<p>On the dev Pi, open <code>~/RPi_recording/ansible/inventory.ini</code> and add a line for the new Pi:</p> <pre><code>[pis]\npi1 ansible_host=192.168.1.101\npi2 ansible_host=192.168.1.102   # add new lines here\n\n[pis:vars]\nansible_user=pi\nansible_ssh_private_key_file=~/.ssh/insect_tracker\n</code></pre> <p>Save the file, then commit and push the change:</p> <pre><code>cd ~/RPi_recording\ngit add ansible/inventory.ini\ngit commit -m \"add pi2 to inventory\"\ngit push\n</code></pre>"},{"location":"setup/add_pi/#15-create-the-unit-config-for-the-new-pi","title":"1.5 Create the unit config for the new Pi","text":"<p>Each Pi has a small config file with its hardware-specific values. Create one for the new Pi:</p> <pre><code>cp ~/RPi_recording/ansible/host_vars/template.yml \\\n   ~/RPi_recording/ansible/host_vars/pi2.yml\n</code></pre> <p>Open <code>pi2.yml</code> and fill in the values for this rig:</p> <pre><code>unit_id: pi2\narena_width_cm: 30\narena_height_cm: 20\nlens_calibration_file: calib_pi2.json   # generated during camera calibration\n</code></pre> <p>Note</p> <p><code>host_vars/</code> is listed in <code>.gitignore</code> \u2014 these files are never committed to GitHub, because they contain hardware-specific values that differ between units.</p>"},{"location":"setup/add_pi/#part-2-bootstrap-via-the-manager-app","title":"Part 2 \u2014 Bootstrap via the Manager app","text":"<p>Once Part 1 is complete, open the Manager app on the dev Pi and use the Add new Pi button, selecting the Pi you just added. The app will:</p> <ol> <li>Install system packages (<code>git</code>, <code>python3-venv</code>, <code>ffmpeg</code>, <code>libcamera-tools</code>)</li> <li>Clone the repository onto the Pi</li> <li>Create a Python virtual environment and install all dependencies</li> <li>Deploy the unit config file</li> </ol> <p>This takes 2\u20135 minutes. The app shows live progress and reports any errors.</p> <p>Adding multiple Pis at once</p> <p>Complete Part 1 for all new Pis first (flash, static IP, SSH key, inventory entry, unit config), then run bootstrapping once \u2014 it targets all unconfigured Pis in one pass.</p>"},{"location":"setup/add_pi/#verify","title":"Verify","text":"<p>After bootstrapping, confirm the new Pi is healthy:</p> <pre><code>ansible pis -i ~/RPi_recording/ansible/inventory.ini -m ping\n</code></pre> <p>All Pis in the inventory should respond with <code>pong</code>. If the new Pi does not respond, re-check the static IP and SSH key steps.</p>"},{"location":"setup/dev_pi/","title":"Step 1: Set up the dev Pi","text":"<p>One-time setup \u2014 most people can skip this page</p> <p>This page is only needed when setting up the dev Pi for the first time, or rebuilding it from scratch. If the dev Pi is already running and the Manager app is accessible, go directly to Add a new Pi.</p> <p>The dev Pi is the central machine for managing the fleet. It runs the Manager app, holds the Ansible configuration, and is the only machine from which code changes are pushed.</p> <p>This setup is done manually \u2014 there is no app to help, because the app itself lives on the dev Pi and does not exist yet at this stage.</p>"},{"location":"setup/dev_pi/#what-you-need","title":"What you need","text":"<ul> <li>Raspberry Pi 4 with SD card (\u2265 32 GB, U30 rated)</li> <li>A computer with Raspberry Pi Imager installed</li> <li>The dev Pi connected to the same network as the recording Pis</li> <li>A GitHub account and a private repo named <code>RPi_recording</code></li> </ul>"},{"location":"setup/dev_pi/#1-flash-the-dev-pi","title":"1. Flash the dev Pi","text":"<ol> <li>Open Raspberry Pi Imager on your computer.</li> <li>Select Raspberry Pi OS (64-bit) as the operating system.</li> <li>Select your SD card.</li> <li>Click the gear icon (advanced options) and set:<ul> <li>Hostname: <code>devpi</code></li> <li>Enable SSH: checked (use password authentication for now)</li> <li>Username: <code>pi</code></li> <li>Set a password you will remember</li> <li>Configure your Wi-Fi network if not using ethernet</li> </ul> </li> <li>Flash the card and insert it into the dev Pi.</li> </ol>"},{"location":"setup/dev_pi/#2-install-git-and-ansible","title":"2. Install Git and Ansible","text":"<p>Open a terminal on the dev Pi (or SSH into it) and run:</p> <pre><code>sudo apt update\nsudo apt install -y git ansible\n</code></pre> <p><code>apt</code> is the Pi's built-in package installer. This installs:</p> <ul> <li>Git \u2014 version control tool for syncing code with GitHub</li> <li>Ansible \u2014 automation tool that manages the other Pis over the network</li> </ul>"},{"location":"setup/dev_pi/#3-generate-an-ssh-key","title":"3. Generate an SSH key","text":"<p>An SSH key is a pair of files that lets the dev Pi connect to GitHub and to all other Pis without typing a password each time. Generate one with:</p> <pre><code>ssh-keygen -t ed25519 -f ~/.ssh/rig_recording\n</code></pre> <p>Press Enter at both prompts to leave the passphrase empty. Two files are created:</p> <ul> <li><code>~/.ssh/rig_recording</code> \u2014 the private key (never share this)</li> <li><code>~/.ssh/rig_recording.pub</code> \u2014 the public key (safe to share)</li> </ul> <p>Tell SSH which key to use for GitHub <pre><code>nano ~/.ssh/config\n</code></pre></p> <p>Add these lines exactly: <pre><code>Host github.com\n    IdentityFile ~/.ssh/rig_recording\n    User git\n</code></pre> Save with <code>Ctrl+O</code>, Enter, then <code>Ctrl+X</code>. Test the connection: <pre><code>ssh -T git@github.com\n</code></pre> You should see: <code>Hi StefanMPopp! You've successfully authenticated...</code></p>"},{"location":"setup/dev_pi/#4-add-the-public-key-to-github","title":"4. Add the public key to GitHub","text":"<p>GitHub needs the public key so that the dev Pi (and later, all other Pis) can pull code without a password.</p> <ol> <li>Print the public key:     <pre><code>cat ~/.ssh/rig_recording.pub\n</code></pre></li> <li>Copy the entire output (one long line starting with <code>ssh-ed25519</code>).</li> <li>On GitHub: go to your <code>RPi_recording</code> repo \u2192 Settings \u2192 Deploy keys \u2192 Add deploy key.</li> <li>Paste the key, give it a name (e.g. <code>devpi</code>), and save.</li> </ol>"},{"location":"setup/dev_pi/#5-clone-the-repository","title":"5. Clone the repository","text":"<p>Download the repository to the dev Pi:</p> <pre><code>git clone git@github.com:StefanMPopp/RPi_recording.git ~/RPi_recording\n</code></pre> <p>This creates a folder at <code>~/RPi_recording</code> containing all project code. This is where you will work and from which all changes are pushed to GitHub.</p>"},{"location":"setup/dev_pi/#6-install-the-manager-app","title":"6. Install the Manager app","text":"<pre><code>cd ~/RPi_recording\npip install -r app/manager/requirements.txt --break-system-packages\n</code></pre> <p>Note</p> <p><code>--break-system-packages</code> is required on Raspberry Pi OS. It does not cause problems \u2014 it just bypasses a safety prompt that is not relevant here.</p>"},{"location":"setup/dev_pi/#done","title":"Done","text":"<p>The dev Pi is now ready. Proceed to Add a new Pi to bring the first recording Pi into the fleet.</p>"},{"location":"setup/overview/","title":"Setup &amp; maintenance overview","text":"<p>This section covers everything managed through the Manager app \u2014 a tool that runs on the dev Pi and handles all software installation and updates across the fleet of recording Pis.</p> <p>Day-to-day users of the recording rigs do not need anything in this section.</p>"},{"location":"setup/overview/#the-three-setup-tasks","title":"The three setup tasks","text":"Task When Tool Set up the dev Pi Once, at the start of the project Manual (follow instructions) Add a new Pi When adding a rig to the fleet Manager app Update Pis When software changes are pushed Manager app"},{"location":"setup/overview/#how-the-system-works","title":"How the system works","text":"<p>All recording Pis run identical software, versioned and stored on GitHub. The Manager app uses Ansible \u2014 a tool that connects to each Pi over the network and enforces that every Pi is in the correct state \u2014 to install or update that software across all Pis simultaneously.</p> <p>The dev Pi is the only machine that runs the Manager app. The other Pis only need to be powered on and connected to the network when a setup or update task runs.</p> <pre><code>Dev Pi  \u2500\u2500(Ansible via SSH)\u2500\u2500\u25ba  Pi 1\n                              \u25ba  Pi 2\n                              \u25ba  Pi 3  \u2026\n</code></pre> <p>See the Add a new Pi page for a full explanation of how this works.</p>"},{"location":"setup/update_pis/","title":"Step 3: Update Pis","text":"<p>Whenever the recording software changes, all Pis need to receive the update. This is done from the Manager app on the dev Pi with a single button press.</p>"},{"location":"setup/update_pis/#when-to-update","title":"When to update","text":"<ul> <li>A bug has been fixed</li> <li>A new feature has been added to the recorder app</li> <li>A Python package has been added or its version has changed</li> </ul> <p>The Pis do not update themselves \u2014 updates only happen when you trigger them.</p>"},{"location":"setup/update_pis/#how-it-works","title":"How it works","text":"<p>Updates follow a fixed sequence:</p> <ol> <li>New code is written and tested on the dev Pi</li> <li>The changes are committed and pushed to GitHub by the developer</li> <li>The Manager app's Update all Pis button pulls those changes onto every Pi</li> </ol> <pre><code>Developer pushes code \u2192 GitHub \u2192 Manager app pulls \u2192 all Pis updated simultaneously\n</code></pre> <p>Only step 3 is relevant here. Steps 1 and 2 are the developer's responsibility.</p>"},{"location":"setup/update_pis/#run-an-update","title":"Run an update","text":"<p>Dev Pi action</p> <p>Updates are triggered from the dev Pi only, via the Manager app. Recording Pis only need to be powered on and connected to the network \u2014 no input is needed on them.</p> <ol> <li>Ensure all Pis are powered on and on the network.</li> <li>Open the Manager app on the dev Pi.</li> <li>Click Update all Pis.</li> </ol> <p>The app connects to each Pi, pulls the latest code from GitHub, and syncs any new dependencies. Only what has changed is transferred \u2014 the entire repository is not re-downloaded each time.</p> <p>To update a single Pi (e.g. after adding it mid-project):</p> <ol> <li>Open the Manager app.</li> <li>Click Update next to the specific Pi.</li> </ol>"},{"location":"setup/update_pis/#if-something-goes-wrong","title":"If something goes wrong","text":"<p>The Manager app shows which Pi failed and the error message from Ansible. Common causes:</p> Symptom Likely cause Fix Pi does not respond Pi is off or not on the network Power it on, wait 60 s, retry Task fails on all Pis Error in the new code or requirements Fix on dev Pi, push, retry Task fails on one Pi Network or disk issue on that Pi SSH into it and inspect manually <p>If a Pi is in a broken state after a failed update, you can re-run the full bootstrap from the Manager app \u2014 it is safe to run on a Pi that already has software installed.</p>"}]}